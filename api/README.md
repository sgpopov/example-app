# Simple API running on Node.js

## Requirements:

- Node.js version 14 or version 15

## Installation

Open a terminal and navigate to this directory. Then run;

```
npm install
```

Once the packages are installed, run the following command to start the dev version:

```
node index.js
```

